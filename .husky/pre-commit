#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "Running lint..."
pnpm lint --max-warnings=0 || exit 1

echo "Running typecheck..."
pnpm exec tsc --noEmit || exit 1

echo "❌ Commit blocked due to lint/type errors"